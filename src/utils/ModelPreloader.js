import React from 'react';
import { useLoading } from '../contexts/LoadingContext';

export const ModelPreloader = () => {
  // Now controlled by LoadingContext
  return null;
};

export default ModelPreloader;